import styled from "styled-components";

export const Panel = styled.nav`
    width: 320px;
    height: 100vh;
    background-color: ${({ theme }) => theme.colors.black};
`;

export const NacItem = styled.a`

`;