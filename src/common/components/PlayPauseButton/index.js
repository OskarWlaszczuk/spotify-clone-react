import { Button } from "./styled"
import { StyledPlayIcon } from "../StyledPlayIcon"

export const PlayPauseButton = () => {
    return (
        <Button>
            <StyledPlayIcon />
        </Button>
    )
}