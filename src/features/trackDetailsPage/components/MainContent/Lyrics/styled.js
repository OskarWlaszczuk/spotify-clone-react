import styled from "styled-components";

export const LyricsSection = styled.article`
    z-index: 2;
    position: relative;
`;

export const LyricsLine = styled.p`
    margin: 0;
    font-size: 14px;
`;